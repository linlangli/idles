<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="280dp"
    android:background="@drawable/dialog_resume_background">

    <EditText
        android:id="@+id/dialog_editText_resume"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="35dp"
        android:background="@drawable/dialog_resume_edit"
        android:inputType="text"
        android:hint="@string/dialog_resume_editText_hint"
        android:gravity="top"
        android:padding="10dp"
        android:textSize="13sp"
        tools:ignore="Autofill" />

    <Button
        android:id="@+id/dialog_resume_button_ok"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        app:layout_constraintTop_toBottomOf="@id/dialog_editText_resume"
        android:layout_margin="10dp"
        android:text="@string/dialog_resume_button"
        android:textColor="@android:color/white"
        android:background="@android:color/black"/>

</androidx.constraintlayout.widget.ConstraintLayout>