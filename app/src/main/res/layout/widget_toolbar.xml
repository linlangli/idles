<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="45dp"
    android:background="@color/toolBar_background"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <EditText
        android:id="@+id/toolbar_editText_search"
        android:layout_width="0dp"
        android:layout_height="25dp"
        android:background="@drawable/tool_bar_edit"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/toolbar_imageButton_scan"
        android:layout_marginEnd="10dp"
        android:hint="@string/toolbar_editText_search_hint"
        android:textSize="12sp"
        android:paddingStart="30dp"
        android:paddingEnd="30dp"
        android:inputType="text"
        android:layout_marginStart="10dp"
        android:importantForAutofill="no" />

    <ImageView
        android:id="@+id/toolbar_imageView_search"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_search"
        app:layout_constraintTop_toTopOf="@id/toolbar_editText_search"
        app:layout_constraintStart_toStartOf="@id/toolbar_editText_search"
        app:layout_constraintBottom_toBottomOf="@id/toolbar_editText_search"
        android:paddingStart="5dp"
        android:paddingEnd="5dp"
        tools:ignore="ContentDescription" />

    <ImageButton
        android:id="@+id/toolbar_imageButton_scan"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_scan"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginEnd="10dp"
        android:background="@android:color/transparent"
        tools:ignore="ContentDescription" />

</androidx.constraintlayout.widget.ConstraintLayout>